Run the following fixes on the project sequentially:

1. `npx prettier --write .` — format all files
2. `npx eslint . --fix` — fix ESLint errors automatically

Report what was changed. If ESLint has errors that cannot be fixed automatically — list them.
